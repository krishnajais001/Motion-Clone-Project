import { CalendarView } from '@/features/calendar/CalendarView';

export default function CalendarPage() {
    return (
        <div className="flex flex-col h-full bg-[#fcfcfc] p-8">
            <CalendarView />
        </div>
    );
}
